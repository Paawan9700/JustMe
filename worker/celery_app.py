"""
Celery application for the JustMe worker.

This file is intentionally empty in Milestone 0. It will be wired up
during Milestone 3 (worker bring-up). The worker is deployed separately
on a GPU host — it is NOT run inside the Emergent preview environment.
"""
