# JustMe Worker — Docker image.
#
# This is a placeholder Dockerfile that will be filled in during the
# worker bring-up milestone (M3+). The worker needs CUDA + ffmpeg +
# WhisperX + pyannote.audio, so the final image will be based on an
# NVIDIA CUDA image. Do not build this in M0.
